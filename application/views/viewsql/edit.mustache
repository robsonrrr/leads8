{{>partials/header-tailwind}}

<div class="min-h-screen bg-gradient-to-br from-slate-50 to-blue-50 py-8">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <!-- Header -->
        <div class="mb-8">
            <div class="flex items-center justify-between">
                <div>
                    <h1 class="text-3xl font-bold text-gray-900 mb-2">
                        ✏️ Editor SQL
                    </h1>
                    <div class="flex items-center space-x-4 text-sm text-gray-600">
                        <span>📁 Editando: <span class="font-semibold text-blue-600">{{filename}}</span></span>
                        {{#view_name}}
                        <span>🗄️ View: <span class="font-mono bg-blue-50 text-blue-700 px-2 py-1 rounded">{{view_name}}</span></span>
                        {{/view_name}}
                        {{^view_name}}
                        <span class="text-amber-600">⚠️ Nome da view não detectado</span>
                        {{/view_name}}
                    </div>
                </div>
                <div class="flex items-center space-x-3">
                    <a href="/leads8/viewsql" class="inline-flex items-center px-4 py-2 bg-gray-600 hover:bg-gray-700 text-white font-medium rounded-lg transition-colors duration-200 shadow-lg">
                        <svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 19l-7-7m0 0l7-7m-7 7h18"></path>
                        </svg>
                        Voltar à Lista
                    </a>
                </div>
            </div>
        </div>

        <!-- File Info Cards -->
        <div class="grid grid-cols-1 md:grid-cols-3 gap-6 mb-8">
            <div class="bg-white rounded-xl shadow-lg p-6 border-l-4 border-blue-500">
                <div class="flex items-center">
                    <div class="bg-blue-100 p-3 rounded-full mr-4">
                        <svg class="w-6 h-6 text-blue-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"></path>
                        </svg>
                    </div>
                    <div>
                        <p class="text-lg font-semibold text-gray-700">Tamanho</p>
                        <p class="text-2xl font-bold text-blue-600">{{file_size_formatted}}</p>
                        <p class="text-sm text-gray-500">{{file_size}} bytes</p>
                    </div>
                </div>
            </div>

            <div class="bg-white rounded-xl shadow-lg p-6 border-l-4 border-green-500">
                <div class="flex items-center">
                    <div class="bg-green-100 p-3 rounded-full mr-4">
                        <svg class="w-6 h-6 text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                        </svg>
                    </div>
                    <div>
                        <p class="text-lg font-semibold text-gray-700">Modificado</p>
                        <p class="text-sm font-bold text-green-600">{{last_modified}}</p>
                        <p class="text-sm text-gray-500">Última alteração</p>
                    </div>
                </div>
            </div>

            <div class="bg-white rounded-xl shadow-lg p-6 border-l-4 border-purple-500">
                <div class="flex items-center">
                    <div class="bg-purple-100 p-3 rounded-full mr-4">
                        <svg class="w-6 h-6 text-purple-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 7v10c0 2.21 3.582 4 8 4s8-1.79 8-4V7M4 7c0 2.21 3.582 4 8 4s8-1.79 8-4M4 7c0-2.21 3.582-4 8-4s8 1.79 8 4"></path>
                        </svg>
                    </div>
                    <div>
                        <p class="text-lg font-semibold text-gray-700">Tipo</p>
                        <p class="text-sm font-bold text-purple-600 bg-purple-50 px-2 py-1 rounded">SQL VIEW</p>
                        <p class="text-sm text-gray-500">Database view</p>
                    </div>
                </div>
            </div>
        </div>

        <!-- Editor Form -->
        <form id="editorForm" method="POST" action="/leads8/viewsql/save">
            <input type="hidden" name="filename" value="{{filename}}">
            <input type="hidden" name="apply_changes" id="applyChanges" value="0">
            
            <!-- Editor Container -->
            <div class="bg-white rounded-xl shadow-lg overflow-hidden">
                <div class="px-6 py-4 border-b border-gray-200 bg-gradient-to-r from-gray-50 to-white">
                    <div class="flex items-center justify-between">
                        <h3 class="text-xl font-semibold text-gray-900 flex items-center">
                            <svg class="w-6 h-6 text-green-500 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 20l4-16m4 4l4 4-4 4M6 16l-4-4 4-4"></path>
                            </svg>
                            📝 Editor SQL com Syntax Highlighting
                        </h3>
                        <div class="flex items-center space-x-2">
                            <div class="hidden md:flex items-center space-x-4 text-sm text-gray-600 bg-gray-100 px-3 py-1 rounded-lg">
                                <span><kbd class="bg-white px-1 rounded text-xs">Ctrl+S</kbd> Salvar</span>
                                <span><kbd class="bg-white px-1 rounded text-xs">Ctrl+Shift+S</kbd> Aplicar</span>
                                <span><kbd class="bg-white px-1 rounded text-xs">F5</kbd> Testar</span>
                            </div>
                        </div>
                    </div>
                </div>
                
                <!-- Action Buttons Bar -->
                <div class="px-6 py-4 bg-gray-50 border-b border-gray-200">
                    <div class="flex flex-wrap items-center gap-3">
                        <button type="button" onclick="testSQL()" class="inline-flex items-center px-4 py-2 bg-gradient-to-r from-yellow-500 to-yellow-600 hover:from-yellow-600 hover:to-yellow-700 text-white font-medium rounded-lg transition-all duration-200 shadow-md hover:shadow-lg transform hover:scale-105">
                            <svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                            </svg>
                            🧪 Testar Sintaxe
                        </button>
                        <button type="button" onclick="saveOnly()" class="inline-flex items-center px-4 py-2 bg-gradient-to-r from-blue-500 to-blue-600 hover:from-blue-600 hover:to-blue-700 text-white font-medium rounded-lg transition-all duration-200 shadow-md hover:shadow-lg transform hover:scale-105">
                            <svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7H5a2 2 0 00-2 2v9a2 2 0 002 2h14a2 2 0 002-2V9a2 2 0 00-2-2h-3m-1 4l-3-3m0 0l-3 3m3-3v12"></path>
                            </svg>
                            💾 Salvar Arquivo
                        </button>
                        <button type="button" onclick="saveAndApply()" class="inline-flex items-center px-4 py-2 bg-gradient-to-r from-green-500 to-green-600 hover:from-green-600 hover:to-green-700 text-white font-medium rounded-lg transition-all duration-200 shadow-md hover:shadow-lg transform hover:scale-105">
                            <svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 7v10c0 2.21 3.582 4 8 4s8-1.79 8-4V7M4 7c0 2.21 3.582 4 8 4s8-1.79 8-4M4 7c0-2.21 3.582-4 8-4s8 1.79 8 4"></path>
                            </svg>
                            🚀 Salvar e Aplicar View
                        </button>
                        <button type="button" onclick="formatSQL()" class="inline-flex items-center px-3 py-2 bg-gradient-to-r from-purple-500 to-purple-600 hover:from-purple-600 hover:to-purple-700 text-white font-medium rounded-lg transition-all duration-200">
                            <svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"></path>
                            </svg>
                            ✨ Formatar
                        </button>
                    </div>
                </div>
                
                <div class="p-6">
                    <textarea id="sqlEditor" name="content" class="w-full h-96 font-mono text-sm border rounded-lg">{{content}}</textarea>
                </div>
            </div>
        </form>

        <!-- Status Panel -->
        <div id="statusPanel" class="hidden mt-6">
            <div class="bg-white rounded-xl shadow-lg p-6 border-l-4 border-blue-500">
                <div class="flex items-center">
                    <div id="statusIcon" class="mr-3"></div>
                    <div>
                        <h3 id="statusTitle" class="text-lg font-semibold text-gray-900"></h3>
                        <p id="statusMessage" class="text-sm text-gray-600 mt-1"></p>
                    </div>
                </div>
            </div>
        </div>

        <!-- Help Panel -->
        <div class="bg-gradient-to-r from-amber-50 to-orange-50 border border-amber-200 rounded-xl p-6 mt-8">
            <div class="flex items-start">
                <svg class="w-6 h-6 text-amber-600 mt-1 mr-3 flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-2.5L13.732 4c-.77-.833-1.964-.833-2.732 0L3.732 16c-.77.833.192 2.5 1.732 2.5z"></path>
                </svg>
                <div>
                    <h3 class="text-lg font-semibold text-amber-900 mb-3">⚠️ Instruções de Segurança</h3>
                    <div class="grid grid-cols-1 md:grid-cols-2 gap-4 text-sm text-amber-800">
                        <div>
                            <p class="font-semibold mb-1">🧪 Testar Sintaxe:</p>
                            <p>Sempre valide a sintaxe SQL antes de aplicar mudanças para evitar erros no banco de dados</p>
                        </div>
                        <div>
                            <p class="font-semibold mb-1">💾 Salvar Arquivo:</p>
                            <p>Salva apenas o arquivo .sql local sem modificar a view ativa no banco de dados</p>
                        </div>
                        <div>
                            <p class="font-semibold mb-1">🚀 Salvar e Aplicar:</p>
                            <p>Salva o arquivo E recria a view no banco. Esta ação afeta o sistema imediatamente!</p>
                        </div>
                        <div>
                            <p class="font-semibold mb-1">🛡️ Backup Automático:</p>
                            <p>Um backup da view atual é criado automaticamente antes de aplicar mudanças</p>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<!-- CodeMirror CSS and JS -->
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/codemirror/5.65.2/codemirror.min.css">
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/codemirror/5.65.2/theme/monokai.min.css">
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/codemirror/5.65.2/theme/material.min.css">
<script src="https://cdnjs.cloudflare.com/ajax/libs/codemirror/5.65.2/codemirror.min.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/codemirror/5.65.2/mode/sql/sql.min.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/codemirror/5.65.2/addon/edit/matchbrackets.min.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/codemirror/5.65.2/addon/hint/show-hint.min.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/codemirror/5.65.2/addon/hint/sql-hint.min.js"></script>
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/codemirror/5.65.2/addon/hint/show-hint.min.css">

<style>
.CodeMirror {
    height: 500px;
    border: 2px solid #e5e7eb;
    border-radius: 0.75rem;
    font-size: 14px;
    font-family: 'SF Mono', Monaco, 'Cascadia Code', 'Roboto Mono', Consolas, 'Courier New', monospace;
}
.CodeMirror-focused {
    border-color: #3b82f6;
    box-shadow: 0 0 0 3px rgba(59, 130, 246, 0.1);
}
.CodeMirror-gutters {
    background: #f8fafc;
    border-right: 1px solid #e5e7eb;
}
.CodeMirror-linenumber {
    color: #9ca3af;
}
</style>

<script>
let editor;

document.addEventListener('DOMContentLoaded', function() {
    // Initialize CodeMirror with advanced features
    const textarea = document.getElementById('sqlEditor');
    editor = CodeMirror.fromTextArea(textarea, {
        mode: 'text/x-sql',
        theme: 'material',
        lineNumbers: true,
        indentUnit: 4,
        smartIndent: true,
        lineWrapping: true,
        matchBrackets: true,
        autoCloseBrackets: true,
        foldGutter: true,
        gutters: ["CodeMirror-linenumbers", "CodeMirror-foldgutter"],
        extraKeys: {
            "Ctrl-Space": "autocomplete",
            "Ctrl-S": function(cm) {
                saveOnly();
                return false;
            },
            "Ctrl-Shift-S": function(cm) {
                saveAndApply();
                return false;
            },
            "F5": function(cm) {
                testSQL();
                return false;
            },
            "Ctrl-/": "toggleComment"
        },
        hintOptions: {
            tables: {
                "vw_leads_produtos": ["lead_id", "cliente_nome", "produto_modelo", "valor_total_item"],
                "inv": ["id", "modelo", "marca", "fob", "peso"],
                "hist": ["isbn", "quant", "valor_base"],
                "next": ["isbn", "quant", "state"]
            }
        }
    });
    
    // Auto-resize editor
    editor.setSize(null, 500);
    
    // Add loading animation
    setTimeout(() => {
        const wrapper = editor.getWrapperElement();
        wrapper.style.opacity = '0';
        wrapper.style.transform = 'translateY(20px)';
        wrapper.style.transition = 'all 0.5s ease';
        setTimeout(() => {
            wrapper.style.opacity = '1';
            wrapper.style.transform = 'translateY(0)';
        }, 100);
    }, 100);
});

function testSQL() {
    const content = editor.getValue();
    
    if (!content.trim()) {
        showStatus('❌ Erro', 'Conteúdo SQL está vazio', 'error');
        return;
    }
    
    showStatus('🧪 Testando...', 'Validando sintaxe SQL...', 'loading');
    
    fetch('/leads8/viewsql/test', {
        method: 'POST',
        headers: {
            'Content-Type': 'application/x-www-form-urlencoded',
        },
        body: 'content=' + encodeURIComponent(content)
    })
    .then(response => response.json())
    .then(data => {
        if (data.success) {
            showStatus('✅ Sintaxe Válida', data.message + (data.view_name ? ` (View: ${data.view_name})` : ''), 'success');
            showNotification(data.message, 'success');
        } else {
            showStatus('❌ Erro de Sintaxe', data.message, 'error');
            showNotification(data.message, 'error');
        }
    })
    .catch(error => {
        showStatus('❌ Erro', 'Erro ao testar SQL: ' + error.message, 'error');
        showNotification('Erro ao testar SQL', 'error');
        console.error('Error:', error);
    });
}

function saveOnly() {
    document.getElementById('applyChanges').value = '0';
    submitForm();
}

function saveAndApply() {
    if (confirm('⚠️ ATENÇÃO: Isso irá recriar a view no banco de dados.\\n\\n✅ Um backup automático será criado antes da modificação.\\n\\nTem certeza que deseja continuar?')) {
        document.getElementById('applyChanges').value = '1';
        submitForm();
    }
}

function formatSQL() {
    // Simple SQL formatting
    let content = editor.getValue();
    content = content
        .replace(/\\bSELECT\\b/gi, 'SELECT')
        .replace(/\\bFROM\\b/gi, 'FROM')
        .replace(/\\bWHERE\\b/gi, 'WHERE')
        .replace(/\\bJOIN\\b/gi, 'JOIN')
        .replace(/\\bLEFT\\s+JOIN\\b/gi, 'LEFT JOIN')
        .replace(/\\bINNER\\s+JOIN\\b/gi, 'INNER JOIN')
        .replace(/\\bORDER\\s+BY\\b/gi, 'ORDER BY')
        .replace(/\\bGROUP\\s+BY\\b/gi, 'GROUP BY')
        .replace(/\\bHAVING\\b/gi, 'HAVING');
    
    editor.setValue(content);
    showNotification('SQL formatado com sucesso!', 'success');
}

function submitForm() {
    // Update textarea with editor content
    editor.save();
    
    const form = document.getElementById('editorForm');
    const formData = new FormData(form);
    
    showStatus('💾 Salvando...', 'Processando alterações...', 'loading');
    
    fetch('/leads8/viewsql/save', {
        method: 'POST',
        body: formData
    })
    .then(response => response.json())
    .then(data => {
        if (data.success) {
            let message = 'Arquivo salvo com sucesso!';
            if (data.view_applied) {
                message += ` View '${data.view_name}' foi recriada no banco de dados.`;
            }
            showStatus('✅ Sucesso', message, 'success');
            showNotification(data.message, 'success');
        } else {
            showStatus('❌ Erro', data.message, 'error');
            showNotification(data.message, 'error');
        }
    })
    .catch(error => {
        showStatus('❌ Erro', 'Erro ao salvar: ' + error.message, 'error');
        showNotification('Erro ao salvar arquivo', 'error');
        console.error('Error:', error);
    });
}

function showStatus(title, message, type) {
    const panel = document.getElementById('statusPanel');
    const icon = document.getElementById('statusIcon');
    const titleEl = document.getElementById('statusTitle');
    const messageEl = document.getElementById('statusMessage');
    
    titleEl.textContent = title;
    messageEl.textContent = message;
    
    // Set icon and colors based on type
    if (type === 'success') {
        icon.innerHTML = '<div class="bg-green-100 p-2 rounded-full"><svg class="w-6 h-6 text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path></svg></div>';
        panel.className = 'bg-white rounded-xl shadow-lg p-6 border-l-4 border-green-500 mt-6';
    } else if (type === 'error') {
        icon.innerHTML = '<div class="bg-red-100 p-2 rounded-full"><svg class="w-6 h-6 text-red-600" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4m0 4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path></svg></div>';
        panel.className = 'bg-white rounded-xl shadow-lg p-6 border-l-4 border-red-500 mt-6';
    } else if (type === 'loading') {
        icon.innerHTML = '<div class="bg-blue-100 p-2 rounded-full"><div class="animate-spin rounded-full h-6 w-6 border-b-2 border-blue-600"></div></div>';
        panel.className = 'bg-white rounded-xl shadow-lg p-6 border-l-4 border-blue-500 mt-6';
    }
    
    panel.classList.remove('hidden');
    
    // Scroll to status panel
    panel.scrollIntoView({ behavior: 'smooth', block: 'nearest' });
}

function showNotification(message, type = 'info') {
    console.log(`Notification: ${message} (${type})`);
    // TODO: Implement toast notifications if needed
}

// Keyboard shortcuts help
document.addEventListener('keydown', function(e) {
    if (e.ctrlKey && e.key === 's') {
        e.preventDefault();
        if (e.shiftKey) {
            saveAndApply();
        } else {
            saveOnly();
        }
    } else if (e.key === 'F5') {
        e.preventDefault();
        testSQL();
    }
});

// Auto-save functionality (every 30 seconds)
setInterval(() => {
    if (editor && editor.getValue().trim()) {
        localStorage.setItem('sql_editor_backup_{{filename}}', editor.getValue());
    }
}, 30000);

// Restore from auto-save on load
window.addEventListener('load', function() {
    setTimeout(() => {
        const backup = localStorage.getItem('sql_editor_backup_{{filename}}');
        if (backup && backup !== editor.getValue()) {
            if (confirm('💾 Encontrado backup automático mais recente. Deseja restaurar?')) {
                editor.setValue(backup);
            }
        }
    }, 1000);
});
</script>
